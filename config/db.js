// var environment = process.env.NODE_ENV || 'development';
// var config = require('../dbcall.js')[environment];
// module.exports = require('mongoose')(config);

// var env = process.env.NODE_ENV = process.env.NODE_ENV || 'development';
